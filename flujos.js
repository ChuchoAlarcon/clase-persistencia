//const fs = require('fs')
class flujos{
    ejemploBuffer(){
        const buffer_vacio = Buffer.alloc(20);
        console.log(buffer_vacio);
    }

    ejemploStream(){
        
    }
}
const f = new flujos();
f.ejemploBuffer()